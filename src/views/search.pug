extends base.pug
include mixins/video


block content
    form
        input(type="text", placeholder="Enter Video Title", name="keyword")
        input(type="submit", value="Search Now")
    div  
        each video in videos
            +video(video)
