extends base.pug

block content
    if(errorMessage)
        span=errorMessage
    form(method="POST")
        input(placeholder="Name" name="name" type="text" requied)
        input(placeholder="Email" name="email" type="email" requied)
        input(placeholder="Username" name="username" type="text" requied)
        input(placeholder="Password" name="password" type="password" requied)
        input(placeholder="Password confirm" name="password2" type="password" requied)
        input(placeholder="Location" name="location" type="text" requied)
        input(type="submit" value="Join Now")
        include partials/social-login
    div.auth-switch
        span Already have an account? 
        a(href="/login") Log in now &rarr;